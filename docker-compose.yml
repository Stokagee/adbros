version: '3.8'

services:
  robot-tests:
    build: .
    image: adbros-demo:latest
    volumes:
      - ./reports:/app/reports
    environment:
      - HEADLESS=true
      - BROWSER=chromium
    command: ["robot", "--outputdir", "reports", "--include", "demo", "tests"]
